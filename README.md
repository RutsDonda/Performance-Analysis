# Performance-Analysis
This project performs data preprocessing, statistical analysis, visualization, and classification on a cricket dataset using Python. It handles missing values,,give accurency, computes descriptive statistics, visualizes relationships using plots, and trains a KNN classifier to predict match types with optimal accuracy.
